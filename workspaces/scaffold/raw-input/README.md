# Raw Input

Original scaffold source material and imported references.

Do not rewrite raw source files in this folder. Put analysis in `discovery/`, background in `context/`, and confirmed intent in `requirements/`.
